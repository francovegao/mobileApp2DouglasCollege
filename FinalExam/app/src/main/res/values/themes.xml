<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FinalExam" parent="Theme.AppCompat.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <item name="android:icon">@drawable/ic_launcher_background</item>
            <item name="logo">@drawable/ic_launcher_foreground</item>
            <item name="displayOptions">useLogo|showTitle</item>
    </style>

    <style name="Theme.FinalExam" parent="Base.Theme.FinalExam" />
</resources>